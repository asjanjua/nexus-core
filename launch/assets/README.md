# Launch Assets

These files are lightweight visual assets for the public launch.

## Suggested Use
- `frame-01-install.svg` for the install/onboarding frame
- `frame-02-dashboard.svg` for the executive dashboard frame
- `frame-03-evidence.svg` for the provenance/evidence frame

## Animation Idea
Show the frames in sequence:
1. install
2. dashboard
3. evidence

That tells the whole product story in three beats.

